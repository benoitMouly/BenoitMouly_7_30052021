export default class User {
    constructor(username, email, password, picture, identification) {
      this.username = username;
      this.email = email;
      this.password = password;
      this.picture = picture;
      this.identification = identification
    }
  }